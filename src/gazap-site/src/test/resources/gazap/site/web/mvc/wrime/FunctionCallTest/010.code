import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$FunctionCallTest$010_txt extends gazap.site.web.mvc.wrime.WrimeWriter {
  private gazap.site.web.mvc.wrime.model.Bean0 bean;

  public W$FunctionCallTest$010_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    this.bean=null;
    super.clear();
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    this.bean=(gazap.site.web.mvc.wrime.model.Bean0)model.get("bean");
  }

  protected void renderContent() throws Exception {
    write(bean.arg2(bean.getHello(),bean.getHello()));
  }
}