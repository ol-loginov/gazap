import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$FunctionCallTest$011_txt extends gazap.site.web.mvc.wrime.WrimeWriter {
  private gazap.site.web.mvc.wrime.model.Bean2 bean;

  private gazap.site.web.mvc.wrime.functor.I18Nizer $i18n;

  public W$FunctionCallTest$011_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    this.bean=null;
    this.$i18n=null;
    super.clear();
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    this.bean=(gazap.site.web.mvc.wrime.model.Bean2)model.get("bean");
    this.$i18n=(gazap.site.web.mvc.wrime.functor.I18Nizer)model.get("functor:i18n");
  }

  protected void renderContent() throws Exception {
    bean.call(bean.getInteger());
    write(bean.call(bean.getString()));
  }
}