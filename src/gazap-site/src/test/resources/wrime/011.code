import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$wrime$011_txt extends gazap.site.web.mvc.wrime.WrimeWriter {
  private gazap.site.web.mvc.wrime.model.Bean2 bean;

  public W$wrime$011_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    this.bean=null;
    super.clear();
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    this.bean=(gazap.site.web.mvc.wrime.model.Bean2)model.get("bean");
  }

  protected void renderContent() throws Exception {
    bean.call(bean.getInteger());
    write(bean.call(bean.getString()));
  }
}