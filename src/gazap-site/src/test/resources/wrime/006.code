import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$wrime$006_txt extends gazap.site.web.mvc.wrime.WrimeWriter {
  private String hello;

  public W$wrime$006_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    super.clear();
    hello=null;
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    hello=model.get("hello");
  }

  protected void renderContent() throws Exception {
    write(hello);
  }
}